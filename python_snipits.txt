\\\\\\\\\\\\\\\\\\\\\\\\\ ARG PARSER  //////////////////////////////////
def main():
    parser = argparse.ArgumentParser(description="CLI tool for managing discovery jobs")
    parser.add_argument("--ip", required=True, help="IP or domain of the server")
    parser.add_argument("--username", required=True, help="Username for authentication")
    parser.add_argument("--password", help="Password for authentication (will prompt if not provided)")
    parser.add_argument("--csv", required=True, help="Path to the CSV file containing input data")
    parser.add_argument("--start-scan", action="store_true", help="Start the job scan after creation")
    parser.add_argument("--max-workers", type=int, default=10, help="Number of concurrent threads for POST requests. Default is 10.")


    args = parser.parse_args()


    ip = args.ip
    username = args.username
    password = args.password or getpass("Enter your password: ").strip()
    csv_path = args.csv